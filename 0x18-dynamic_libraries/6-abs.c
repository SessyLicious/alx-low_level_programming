#include "main.h"

/**
 * _abs - A function that computes the absolute value of an integer
 * @n: Its the input to the function
 * Return: n
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
